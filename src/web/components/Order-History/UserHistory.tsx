import { ToolsOrderHistory } from "./ToolsOrderHistory";

export const UserHistory: React.FC = () => {
  // use sortType to fetch data and send it to orders
  // Show only customer orders
  return <ToolsOrderHistory />;
};
